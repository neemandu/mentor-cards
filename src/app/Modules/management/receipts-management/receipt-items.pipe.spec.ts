import { ReceiptItemsPipe } from './receipt-items.pipe';

describe('ReceiptItemsPipe', () => {
  it('create an instance', () => {
    const pipe = new ReceiptItemsPipe();
    expect(pipe).toBeTruthy();
  });
});
