import { TooltipListViewPipe } from './tooltip-list-view.pipe';

describe('TooltipListViewPipe', () => {
  it('create an instance', () => {
    const pipe = new TooltipListViewPipe();
    expect(pipe).toBeTruthy();
  });
});
